<p>こんにちは <%= @resource.name %> さん！</p>

<p>誰かがあなたのパスワードを変更するためのリンクをリクエストしました。以下のリンクから変更できます。</p>

<p><%= link_to 'パスワード変更', edit_password_url(@resource, reset_password_token: @token) %></p>

<p>もしこのリクエストを行っていない場合は、このメールを無視してください。</p>
<p>上記のリンクにアクセスして新しいパスワードを作成するまで、あなたのパスワードは変更されません。</p>